<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.stone.lfernandosantos.storewars">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".views.SplashActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".views.MainActivity" />
        <activity android:name=".views.CartActivity" />
        <activity android:name=".views.CardDataActivity" />
        <activity
            android:name=".views.DetailActivity"
            android:label="@string/title_activity_detail"
            android:theme="@style/AppTheme.NoActionBar" />
        <activity
            android:name=".views.HistoricoActivity"
            android:label="@string/title_activity_historico"
            android:theme="@style/AppTheme.NoActionBar" />
        <activity
            android:name=".views.ListCardsActivity"
            android:label="@string/title_activity_list_cards"
            android:theme="@style/AppTheme.NoActionBar"></activity>
    </application>

</manifest>